<?php

/**
 * PHPStan bootstrap stub.
 * Declares runtime constants and globals so PHPStan can resolve them
 * without actually executing the full application bootstrap.
 */

if (!defined('BASE_URL')) {
    define('BASE_URL', 'http://localhost:8080/myPortfolio/');
}
